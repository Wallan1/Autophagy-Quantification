---
title: "12-Load-PrelimNucleiCount-dataset"
output: html_notebook
---

Load dataframe
*change input file*
*change replicate #*
```{r}
testdata=read.csv(file="~/Desktop/23Jul20_MOC_Results/run2/21Feb20_eqCellSum-MOC-sumInt_basal_23Jul20.csv",header=TRUE)
testdata$BvsS<-"Basal"
testdata$Rep<-1

testdata2=read.csv(file="~/Desktop/23Jul20_MOC_Results/run2/21Feb20_eqCellSum-MOC-sumInt_starved_23Jul20.csv",header=TRUE)
testdata2$BvsS<-"Starved"
testdata2$Rep<-1
```

Adding type designation
*if necessary update substring values based on filename in input document*
```{r}
temp$Type<-NA
ntemp<-nrow(temp)


for (c in 1:ntemp) {
  if (substr(temp[c,1],40,40)=="W") {
    temp$Type[c]<-"WT"
  }
  else if (substr(temp[c,1],40,40)=="M") {
    temp$Type[c]<-"Mock"
  }
  else if (substr(temp[c,1],40,40)=="C") {
    temp$Type[c]<-"Control"
  }
  else if (substr(temp[c,1],40,40)=="t") {
    temp$Type[c]<-"tepsin"
  }
  else {
    temp$Type[c]<-"AP4E1"
  }
  
}
temp$Type<-factor(temp$Type, levels=c("WT", "Mock", "Control", "tepsin", "AP4E1"))
```

Get Sum of cell counts per type
```{r}
temp$CellSum<-NA

for (t in c("WT","Mock", "Control", "tepsin", "AP4E1")) {
  for (bs in c("Basal", "Starved")) {
  temp$CellSum[which(temp$Type==t & temp$BvsS==bs)]<-sum(temp$NucleiCount[which(temp$Type==t & temp$BvsS==bs)])
   }
}
```

